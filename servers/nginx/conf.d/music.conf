server {
    listen 443 ssl;
    http2 on;
    server_name music.henrique.pw;

    location / {
        set $upstream http://navidrome:4533;
        proxy_pass $upstream$request_uri;
        include /etc/nginx/snippets/proxy.conf;
    }
}
