server {
    listen 443 ssl;
    http2 on;
    server_name fileflows.henrique.pw;

    location / {
        set $upstream http://fileflows:5000;
        proxy_pass $upstream$request_uri;
        include /etc/nginx/snippets/proxy.conf;
    }
}
