server {
    listen 443 ssl;
    http2 on;
    server_name dns.henrique.pw;

    location / {
        set $upstream http://adguard:80;
        proxy_pass $upstream$request_uri;
        include /etc/nginx/snippets/proxy.conf;
    }
}
