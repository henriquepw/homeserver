server {
    listen 443 ssl;
    http2 on;
    server_name manga.henrique.pw;

    location / {
        set $upstream http://komga:25600;
        proxy_pass $upstream$request_uri;
        include /etc/nginx/snippets/proxy.conf;
    }
}
